---
title: "Mi manuscrito en APA 7"
author: "Autor"
affiliation: "Universidad Autónoma de Santo Domingo (UASD)"
output:
  bookdown::pdf_document2:
    latex_engine: xelatex
    fig_caption: true
    extra_dependencies: ["float", "booktabs", "longtable"]
documentclass: apa7
classoption: [man,floatsintext]
fontsize: 12pt
lang: es-ES
bibliography: bibliografia.bib
date: "`r format(Sys.Date(), '%Y-%m-%d')`"
header-includes:
  - \title{Mi manuscrito en APA 7}
  - \shorttitle{Mi manuscrito}
  - \affiliation{Universidad Autónoma de Santo Domingo (UASD)}
  - \abstract{ESCRIBE AQUÍ TU RESUMEN. NO INTRODUZCAS ENTER NI SALTOS DE LÍNEA.}
  - \keywords{palabra clave 1, palabra clave 2, palabra clave 3}

  - \usepackage{orcidlink}
  # - \usepackage{float}
  - \usepackage[all]{nowidow}
  - \usepackage{pdflscape}
  - \renewcommand\spanishtablename{Tabla}
  - \usepackage{xcolor}
  - \usepackage{tabu}
  - \usepackage{xurl}
  # - \usepackage[left]{lineno}
  # - \linenumbers
---

\pagebreak

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  cache = FALSE,
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  dpi = 300,
  fig.align = "center",
  out.width = "100%"
)

options(
  knitr.duplicate.label = "allow",
  knitr.kable.NA = ""
)

# Aleatorización
set.seed(99)

# Paquetes
library(tidyverse)
library(knitr)
library(kableExtra)

# Tema general para gráficos ggplot2
theme_set(theme_bw())
```



```{r, echo=F, eval=T, include=F}
# Cargar paquetes
library(cluster)
library(tidyverse)
library(readxl)
library(vegan)

# Cargar datos e imprimirlos
datos <- read_excel("data/sudokus_micro_sistematica.xlsx", sheet = 1, range = "A4:J13", col_names = T)
datos_9x9 <- datos[1:9, 2:10]
datos_9x9

# Estructura del objeto datos_9x9
str(datos_9x9)

# Convertir a matriz numérica si hace falta
mat <- as.matrix(datos_9x9)
mat

# Opción A: Euclidiana (básica)
dist_mat_e <- dist(mat, method = "euclidean")
dist_mat_e

# Opción B: Bray-Curtis (recomendada en ecología)
dist_mat_b <- vegdist(mat, method = "bray")

# Cluster con matriz de distancia euclidea y método UPGMA
hc_upgma_d_e <- hclust(dist_mat_e, method = "average")
hc_upgma_d_e

# Mostrar árbol completo (sin cortar)
plot(hc_upgma_d_e, 
     main = "Dendrograma - Cluster jerárquico",
     xlab = "",
     sub = "",
     cex = 0.8)

# Cortar y poner rectángulos
cutree(hc_upgma_d_e, 2)
rect.hclust(hc_upgma_d_e, 2)
```


```{r, echo=F, eval=T, include=F}
# Por medio de función
source('generar-dendrogramas.R')

# ============================================================
# EJEMPLOS DE USO
# ============================================================

# Ejemplo 1: distancia euclídea
res1 <- analizar_sudoku(
  archivo = "data/sudokus_micro_sistematica.xlsx",
  codigo  = "SK  (I)",
  hoja    = 1,
  k       = 2,
  distancia = "euclidean",
  metodo  = "average",
  graficar = TRUE
)

# Ver matriz extraída
res1$matriz

# Ver grupos asignados a las filas
res1$grupos

# ------------------------------------------------------------

# Ejemplo 2: Bray-Curtis
res2 <- analizar_sudoku(
  archivo = "data/sudokus_micro_sistematica.xlsx",
  codigo  = "CD (IV)",
  hoja    = 1,
  k       = 2,
  distancia = "bray",
  metodo  = "average",
  graficar = TRUE
)

res2$matriz
res2$grupos
```


# Introducción {-}

El Sudoku ...

El análisis de agrupamiento o análisis cluster ... []

# Materiales y métodos {-}



# Resultados {-}

Este es mi dendrograma para el Sudoku SK (I) (ver Figura \@ref(fig:dendrograma-sk-i))

```{r dendrograma-sk-i, echo=F, fig.cap="Dendrograma para el Sudoku SK (I)", fig.pos='H'}
res1 <- analizar_sudoku(
  archivo = "data/sudokus_micro_sistematica.xlsx",
  codigo = "SK  (I)",
  k = 2,
  distancia = "euclidean",
  metodo = "average",
  graficar = TRUE,
  motor_grafico = "base"
)
```

```{r dendrograma-sk-ii, echo=F, fig.cap="Dendrograma para el Sudoku SK  (II)", fig.pos='H'}
res1 <- analizar_sudoku(
  archivo = "data/sudokus_micro_sistematica.xlsx",
  codigo = "SK  (II)",
  k = 2,
  distancia = "euclidean",
  metodo = "average",
  graficar = TRUE,
  motor_grafico = "dendextend"
)
```

```{r dendrograma-sk-iii, echo=F, fig.cap="Dendrograma para el Sudoku SK  (III)", fig.pos='H'}
res1 <- analizar_sudoku(
  archivo = "data/sudokus_micro_sistematica.xlsx",
  codigo = "SK  (III)",
  k = 2,
  distancia = "euclidean",
  metodo = "average",
  graficar = TRUE,
  motor_grafico = "dendextend"
)
```


Este es el dendrograma para el Sudoku CD (IV), usando la distancia Bray-Curtis y cortado en tres grupos usando el paquete `dendextend` (ver Figura \@ref(fig:dendrograma-cd-iv))

```{r dendrograma-cd-iv, echo=F, fig.cap="Dendrograma para el Sudoku CD (IV)", fig.pos='H'}
res2 <- analizar_sudoku(
  archivo = "data/sudokus_micro_sistematica.xlsx",
  codigo = "CD (IV)",
  k = 3,
  distancia = "bray",
  metodo = "average",
  graficar = TRUE,
  motor_grafico = "dendextend"
)
```


# Discusión {-}



# Referencias {-}
